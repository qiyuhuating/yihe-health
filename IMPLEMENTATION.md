# 颐和 · 智慧康养改版说明

这是基于原仓库提交 `a3de36fed6a6773632e1ae852c25fe662280ce2d` 的静态演示改版副本。

已完成：

- 居民端统一进入 `index.html`，旧 `个人端.html` 保留兼容跳转。
- 修复新首页呼救按钮失联，改为独立的“模拟求助”本机事件，不篡改心率数据、不发送通知。
- 个人资料、聊天记录、提醒、联系人和行程按用户 ID 隔离。
- 新增本机提醒任务：内容、时间、编辑、删除、到期提示均有明确状态。
- 用药档案不再按药品排列顺序推断服用时间；未核对时显示警告。
- 周报和月报按北京时间的自然周/月筛选带完整时间戳的本机模拟采样。
- AI 设置增加健康上下文开关、清除密钥入口和模拟身份说明。
- 留言、预约、联系人、通知等未接入服务均改为明确的本机演示文案。
- 页面颜色、字体、按钮、卡片、移动端网格和管理端窄屏布局统一为更克制的中国风系统。
- 移除部署树中的空 REST 文件、未加载健康模块、旧农历脚本和孤立联系人脚本；历史原型移至 `历史设计样例/`。

验证：所有 `js/*.js` 已通过 `node --check`；主要页面通过本地 HTTP 服务返回 200。浏览器自动化回归因当前执行环境的自动审批额度限制未能在改版副本上重新运行，原版本的浏览器审查证据仍保留在上级 `outputs/*audit.json` 文件中。

## 本地预览

```powershell
python -m http.server 8000 --directory outputs/yihe-health
```

打开 `http://127.0.0.1:8000/index.html`。演示登录账号仍使用居民姓名和密码 `123456`；管理端账号为 `admin`，密码 `admin123`。这些账号只适用于静态演示，不能用于生产。
